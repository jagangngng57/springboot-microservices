server:
  port: 9191

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jagangngng57/config-server
          clone-on-start: true